# Notask Flow 前端需求说明书

**项目名称**：Notask Flow – 个人云笔记与团队任务协同平台  
**版本**：2.1  
**日期**：2026-04-25  
**关联后端版本**：V1.5 正式版  
**用途**：供设计师绘制高保真原型、前端开发实现、AI 生成设计图参考

---

## 1. 引言

### 1.1 项目背景

Notask Flow 是一站式效率工具，整合云笔记管理与团队任务协作。前端需提供直观、响应迅速的单页应用（SPA），支持个人空间与团队空间的无缝切换，实现笔记、任务、待办、通知、统计等核心功能。

### 1.2 项目目标

- 构建功能完整、用户体验流畅的 Web 前端
- 严格遵循后端 RESTful API 规范，确保正确的权限校验与数据交互
- 支持移动端适配（响应式布局）
- 提供清晰的代码结构与工程化配置，便于维护和迭代

### 1.3 适用范围

本文档适用于前端开发、UI 设计、测试人员，以及前后端联调。

---

## 2. 技术栈

| 类别        | 技术/库               | 版本/说明                  |
| ----------- | --------------------- | -------------------------- |
| 核心框架    | Vue 3                 | 3.4+                       |
| 构建工具    | Vite                  | 5.0+                       |
| 状态管理    | Pinia                 | 3.0.+                      |
| 路由        | Vue Router            | 4.2+                       |
| UI 组件库   | Element Plus          |                            |
| HTTP 客户端 | Axios                 |                            |
| 富文本编辑  | TipTap / Vditor       | 支持 Markdown 所见即所得   |
| 图表库      | ECharts               | 5.5+（统计仪表盘）         |
| 动画库      | GSAP + Vue Transition | GSAP 3.12+（复杂交互动画） |
| CSS         | Tailwind              |                            |
| 工具库      | Day.js, Lodash        | 最新稳定版                 |
| 代码规范    | ESLint + Prettier     | 遵循 Airbnb 风格（可定制） |
| 版本控制    | Git                   |                            |



---

## 0. 全局空间切换路口

- **位置**：顶部栏右侧，用户头像左侧，始终可见。
- **样式**：圆角胶囊按钮（圆角 32px，内边距 6px 12px）。左侧显示空间类型图标：个人空间为 📘 或 👤，团队空间为 👥 或 🏢。中间显示当前空间名称（最多 15 个字符，超出省略）。右侧向下箭头（▼）。悬停时背景变为浅灰色。
- **类型微提示**：个人空间时胶囊右侧边框有极细暖色（#FF8A65），团队空间时为品牌蓝紫色（#6366f1）。
- **下拉面板**（点击展开）：
  - 宽度 280px，圆角 12px，白色背景，阴影。位于切换器下方。
  - 上方固定区域：“📘 我的笔记空间”（个人空间）。右侧显示蓝色勾图标表示当前选中（如果是当前空间）。
  - 分隔线（浅灰色）。
  - 团队空间列表：每个空间项显示 👥 图标、空间名称、成员数量（例如“5人”），如有未读任务数则显示红点数字。底部有“+ 新建团队空间”链接和“加入团队”按钮。
  - 空状态：若没有团队空间，显示“暂无团队空间，点击创建”，并显示新建按钮。
  - 当前空间高亮（浅蓝背景 #EEF2FF，文字品牌色）。
- **切换动画**：点击新空间后，下拉面板关闭。主内容区旧内容淡出并向左滑动 8px（0.2s），新内容淡入并从右滑动 8px 归位（0.25s）。同时整体主题（色彩、圆角、布局）平滑过渡（GSAP 插值 CSS 变量）。

---

## 3. 整体布局与空间感知

### 3.1 个人空间 UI

**整体感受**：温暖、柔和、私密、专注，类似数字日记或写作软件。支持用户自定义主题（亮色/暗色/森林/海洋等预设）。

#### 3.1.1 顶部栏

- 左侧：Logo + “Notask Flow” 文字。
- 中间：全局搜索框，占位符“搜索笔记、任务、待办..”，圆角 20px，浅灰背景，放大镜图标。支持快捷键 `Ctrl+K` 唤起。
- 右侧：空间切换器、通知图标（铃铛，带未读红点）、用户头像（圆形，下拉菜单含“个人设置”“退出登录”）。

#### 3.1.2 侧边栏

- 宽度 260px，背景色 #FDF8F5（暖白微粉），可折叠至 64px（仅显示图标）。
- **主菜单**：图标+文字：笔记、任务（仅显示个人待办，不显示看板）、待办、统计、通知。
- **笔记本树**：展开/折叠的文件夹列表，支持新建笔记本、重命名、删除、拖拽排序。文件夹图标为 📁。
- **标签区**：常用标签列表（可选）。
- **回收站**：链接，显示已删除笔记数量。
- **底部小部件**（可选）：“随机漫步”按钮（随机打开一篇旧笔记）、“每日一句”（励志格言）。

#### 3.1.3 笔记主页（核心页面）

- **三栏布局**：
  - **左侧笔记本树**：同侧边栏描述。
  - **中间笔记列表**：白色背景，顶部有搜索框（“搜索笔记”）和“+ 新建笔记”按钮。笔记列表每项显示：标题（加粗）、最后更新时间（如“今天 10:30”）、标签小圆点。悬停时右侧出现“…”菜单（编辑、移动、删除、复制链接）。支持多选（复选框）和批量操作。
  - **右侧笔记编辑器**：顶部工具栏（面包屑导航、分享/历史版本/标签/关联任务图标按钮；编辑器模式切换：编辑/预览；格式化按钮：H1/H2/H3/粗体/斜体/列表/引用/代码块）。编辑区默认双栏：左侧 Markdown 源码区（等宽字体，行号，语法高亮），右侧实时预览区（渲染后富文本）。拖拽可调整左右宽度。底部状态栏：字数统计、行数、当前格式（Markdown）、自动保存状态（绿色打勾+时间）、大纲展开按钮（点击弹出标题层级）。历史版本侧边栏（从右侧滑出）显示版本时间轴；标签侧边栏显示空间内所有标签；任务关联侧边栏显示当前空间任务列表。
- **拖拽**：笔记卡片可拖拽到左侧笔记本树的目标文件夹（拖拽时显示半透明卡片+蓝色插入线）。笔记本树节点支持拖拽重排序。

![个人空间-笔记主页](E:\Codex\Notask-Flow\img\个人空间-笔记主页.png)

#### 3.1.4 任务/待办页面（个人）

- **入口**：侧边栏“待办”菜单，或顶部“任务”菜单（实际为个人待办）。
- **布局**：左侧概览 + 右侧列表。
- **左侧**：
  - 统计卡片（圆形或卡片式）：全部待办总数、今日待办数、已完成数、逾期数。
  - 快速筛选：我创建的、今日到期、本周到期、逾期、无截止时间。
  - 标签筛选（彩色标签列表，显示数量）。
  - 来源筛选：仅“手动创建”（无“任务生成”）。
- **右侧**：
  - 顶部：搜索框（搜索待办标题）、筛选下拉（全部/未完成/已完成）、排序（按截止时间）、新建待办按钮。
  - 待办列表项：左侧圆形复选框（空心圆 → 带勾实心圆）；中间待办标题（完成时带删除线）、截止时间（红色表示逾期，格式“今天 18:00”或“06-04”）、来源标签（“手动创建”，浅绿色背景）；右侧编辑和删除图标。已完成项显示“完成于 05-31”。
  - 分页（10/20/50 条每页）。
- **新建待办弹窗**：标题（必填）、截止时间（可选）、所属空间（默认当前个人空间）、备注（可选）。

![个人空间-待办页面](E:\Codex\Notask-Flow\img\个人空间-待办页面.png)

#### 3.1.5 个人统计页面

- **布局**：简单卡片式仪表盘。

- **内容**：

  - 笔记总数（卡片，带图标 📝）
  - 未完成任务数（带图标 ✅）
  - 本月完成任务数（带图标 📅）
  - 可选：近 7 天笔记创建/编辑趋势（迷你折线图）。

  ![个人空间-统计页面](E:\Codex\Notask-Flow\img\个人空间-统计页面.png)

#### 3.1.6 个人设置页面

- **左侧二级菜单**：个人资料、账户安全、主题与偏好、我的团队、帮助与反馈、退出登录。

- **右侧内容**：

  - **个人资料**：头像（圆形预览，点击上传，支持 JPG/PNG ≤2MB）、昵称（可编辑）、用户名（不可修改，提示用于登录和@提及）、邮箱（可编辑，需验证）。保存修改按钮。
  - **账户安全**：修改密码（旧密码、新密码、确认新密码）、注销账户（警告文字+二次确认弹窗）。
  - **主题与偏好**：主题模式（亮色/暗色/跟随系统）、个人空间主题预设（暖阳/森林/海洋/暗夜，预览色块）；侧边栏偏好（始终展开/自动折叠/始终折叠）；通知偏好（任务提醒/协作通知/系统通知/邮件通知开关）。
  - **我的团队**：展示当前用户已加入的所有团队（名称、角色、成员数），每个团队旁边显示“退出团队”按钮（所有者不能退出，只能转让或解散），以及“切换至”按钮。底部提供“加入团队”按钮（弹窗输入邀请码或申请）。

  ![个人空间-个人设置](E:\Codex\Notask-Flow\img\个人空间-个人设置.png)

### 3.2 团队空间 UI

**整体感受**：冷静、高效、专业，适合多人协作与项目管理。固定品牌蓝紫色 + 浅灰背景，不允许用户自定义主题。

#### 3.2.1 顶部栏

- 左侧：Logo + “Notask Flow”。
- 中间：全局搜索框（搜索任务、笔记、待办）。
- 右侧：**空间切换器**（见第 0 节）、**成员头像组**（最多显示 5 个头像小圆圈，鼠标悬停显示姓名和角色，更多成员显示“+N”）、**通知图标**（带未读红点）、**用户头像**（下拉菜单含个人设置、退出登录）。

#### 3.2.2 侧边栏

- 宽度 260px，#F2F4F7（浅灰冷调），可折叠。
- **主菜单**：团队空间、项目、任务看板（看板视图）、协作文档、报表、成员。
- **任务看板分组**：自定义保存的看板视图列表（例如“产品看板”“开发看板”）。
- **成员在线状态**：小区域展示空间成员在线离线头像圆圈，点击可快速@。
- **近期团队动态**：迷你时间线（如“张小明 完成任务 A”），可点击查看更多。
- **团队共享文档树**：共享文档列表（与个人空间类似，但所有成员可见）。

#### 3.2.3 项目模块

项目是团队空间内组织任务和文档的高级分组单元，用于管理大型工作、产品迭代或部门工作。

**入口**：侧边栏“项目”菜单，点击进入项目列表页。

**项目列表页**：
- 布局：网格视图（默认）或列表视图（可切换），展示项目卡片。
- 每个项目卡片包含：项目名称、描述（截断）、任务进度（环形进度条或文本如“12/30 已完成”）、成员头像组（最多显示3个）、操作菜单（编辑、归档、删除）。
- 顶部操作栏：搜索框（搜索项目名称）、视图切换、新建项目按钮。
- 空状态：插画 + “创建第一个项目”按钮。

![团队空间-项目页面](E:\Codex\Notask-Flow\img\团队空间-项目页面.png)

**新建项目弹窗**：

- 项目名称（必填，2-30字符）
- 项目描述（可选，多行文本）
- 项目封面（可选，上传图片或选择颜色，默认品牌色）
- 项目负责人（默认当前用户，可下拉选择空间成员）
- 创建后自动进入项目详情页。

![团队空间-项目详情-新建项目](E:\Codex\Notask-Flow\img\团队空间-项目详情-新建项目.png)

**项目详情页**：布局为顶部项目信息区 + Tab栏 + 内容区。

- **顶部项目信息区**：项目名称（可编辑）、描述（可编辑）、封面背景（可更换）、项目进度百分比、成员头像组、项目设置（齿轮图标，仅项目负责人/空间管理员可见）。
- **Tab栏**：概览、任务看板、文档、成员。
  - **概览 Tab**：
    - 左侧：项目统计卡片（总任务数、已完成任务数、逾期任务数、文档数）。
    - 右侧：环形图展示任务完成率。
    - 下方：近期活动时间线（成员完成任务、创建文档、添加评论等）。
    - 项目描述富文本展示区（可编辑）。
    
    ![团队空间-项目详情](E:\Codex\Notask-Flow\img\团队空间-项目详情.png)
  - **任务看板 Tab**：
    - 复用团队空间的任务看板组件，但自动筛选该项目关联的任务（任务创建/编辑时可选所属项目）。
    - 看板顶部显示项目名称和筛选条件，支持按状态、责任人等筛选。
    - 任务卡片上显示项目标签（可选）。
    - 支持批量移动任务到其他项目。
    
    ![团队空间-项目详情-任务看板](E:\Codex\Notask-Flow\img\团队空间-项目详情-任务看板.png)
  - **文档 Tab**：
    - 展示该项目关联的协作文档列表（文档卡片：标题、最后编辑时间、编辑者）。
    - 顶部有“新建文档”按钮，新建时自动关联本项目。
    - 支持为已有文档批量关联/取消关联项目。
    
    ![团队空间-项目详情-文档](E:\Codex\Notask-Flow\img\团队空间-项目详情-文档.png)
  - **成员 Tab**：
    
    - 项目成员列表（头像、姓名、角色：项目负责人/成员）。
    - 项目负责人有权限添加/移除项目成员（只能从空间成员中选择）。
    - 空间管理员/所有者可覆盖任何项目成员操作。
    - 支持设置项目负责人的角色（至少一名负责人，可多人）。
    
    ![团队空间-项目详情-成员](E:\Codex\Notask-Flow\img\团队空间-项目详情-成员.png)

**项目与任务/文档的关系**：

- 任务创建/编辑界面增加“所属项目”下拉选择框（当前空间下的项目列表）。
- 文档创建/编辑界面增加“所属项目”下拉选择框。
- 在团队空间的任务看板、协作文档模块的筛选栏中，增加“项目”筛选下拉，方便快速查看特定项目的内容。

**项目设置（抽屉或单独页面）**：
- 编辑项目名称、描述、封面。
- 归档项目（归档后任务/文档不再显示在默认视图中，但可取消归档）。
- 删除项目（二次确认，删除时需转移或删除关联的任务/文档，提供选项）。
- 项目概览中支持“复制项目链接”用于分享。

![团队空间-项目详情-项目设置](E:\Codex\Notask-Flow\img\团队空间-项目详情-项目设置.png)

**权限说明**：

- 项目继承空间权限：空间成员默认可以查看所有项目（只读访客只能查看不可编辑）。
- 项目负责人（由空间管理员/所有者或项目创建者指定）可以：
  - 编辑项目基本信息
  - 管理项目成员（添加/移除）
  - 归档/删除项目（删除需空间管理员确认）
- 空间所有者/管理员可以管理所有项目（编辑、删除、调整负责人）。

**路由**：
- `/app/projects` - 项目列表
- `/app/projects/:projectId` - 项目详情（默认概览）
- `/app/projects/:projectId/tasks` - 项目任务看板
- `/app/projects/:projectId/docs` - 项目文档



#### 3.2.4 任务看板（核心页面）

- **布局**：全宽，可滚动。
- **顶部筛选栏**：
  - 空间名称（如“产品研发部”）。
  - 视图切换按钮：列表视图 / 看板视图（默认看板）。
  - 筛选条件：状态（全部/OPEN/IN_PROGRESS/COMPLETED/CANCELLED）、模式（全部/指派/开放）、责任人（全部/具体成员）、项目（全部/具体项目）。
  - 搜索框（搜索任务标题或内容）。
  - “新建任务”按钮（主色，下拉可选择指派模式或开放认领模式）。
- **看板列**：四列并列（OPEN / IN_PROGRESS / COMPLETED / CANCELLED）。每列头部显示状态名称和任务数量（例如“进行中 · 3”）。列宽可拖拽调整。支持自定义分组保存为个人视图或团队视图。
- **任务卡片**：
  - 白色卡片，圆角 8px，边框 1px solid #e2e8f0，无阴影。卡片内边距 12px。
  - 上方行：任务标题（加粗，14px）、优先级标签（高/中/低，带背景色，例如红色/橙色/灰色）。
  - 中间行：截止时间（红色表示逾期）、进度百分比（如“60%”）、所属项目标签（浅灰色背景，可选）。
  - 底部行：责任人头像组（小圆圈，最多 3 个）、状态徽章（进行中/待认领等）。
  - 悬停时边框变品牌色，右上角显示操作菜单（开始/完成/取消/编辑）。
- **拖拽**：卡片可拖拽到其他列（自动调用后端状态变更），拖拽时半透明，目标列显示蓝色虚线边框。
- **任务详情抽屉**（点击卡片从右侧滑出，宽度 520px）：
  - 顶部栏：任务标题（可编辑）、状态徽章、优先级下拉、所属项目下拉（可切换项目）、关闭按钮。
  - 基本信息：描述（富文本）、截止时间（日期选择器）、创建人/创建时间。
  - **成员职责表**：表格或列表，每行：头像+姓名、角色（负责人/协作者）、职责描述、进度（百分比滑动条/数字）、完成状态（勾选框）。支持添加/删除成员（需权限）。每个成员旁有“开始/完成”按钮（仅责任人可见）。
  - **评论时间线**：每条评论显示头像、用户名、时间、内容（支持富文本、@提及、代码块）。回复嵌套缩进。底部输入框（支持 Ctrl+Enter 发送）。
  - **相关笔记与附件**：展示已关联的笔记标题（链接，可跳转）、附件列表（文件名、大小、下载图标）。带“添加”按钮（选择笔记本树中的笔记或上传文件）。

![团队空间-任务看板](E:\Codex\Notask-Flow\img\团队空间-任务看板.png)

#### 3.2.5 协作文档模块（团队空间）

- 与个人空间类似，但去掉“随机漫步”等个人化组件，增加“协作者头像组”显示最近编辑者。
- 笔记本树为团队共享，所有成员可见（根据权限可能只读或可编辑）。
- 编辑器不居中，最大宽度 100%，紧凑布局。
- 历史版本和标签功能相同。
- 文档列表顶部增加“项目”筛选下拉。

![团队空间-协作文档](E:\Codex\Notask-Flow\img\团队空间-协作文档.png)

#### 3.2.6 待办页面（团队空间）

- **布局**：左侧概览 + 右侧列表（同个人空间，但筛选更丰富）。
- **左侧**：
  - 统计卡片：全部待办（包括任务生成的）、今日待办、未完成、本周到期。
  - 快速筛选：我创建的、分配给我的、已逾期、即将到期、无截止时间。
  - 标签筛选（空间级标签）。
  - 来源筛选：任务生成（来自任务模块）、手动创建（复选框）。
- **右侧**：
  - 待办列表项：与个人空间类似，但来源标签显示“任务生成”（蓝色）或“手动创建”（绿色）。来源为任务生成时，右侧显示“查看任务”链接。
  - 支持分配给人筛选（下拉选择成员）。

![团队空间-待办页面](E:\Codex\Notask-Flow\img\团队空间-待办页面.png)

#### 3.2.7 统计仪表盘（团队空间）

- **布局**：全宽，图表区网格排列。
- **内容**：
  - **成员任务负载**：水平条形图，Y轴成员姓名，X轴任务数（进行中+未开始），叠加折线图表示已完成任务数。
  - **近7天任务完成趋势**：双轴折线图（创建任务数 vs 完成任务数），日期为 X 轴。
  - **按角色完成任务占比**：环形饼图（空间所有者、管理员、成员等角色完成的百分比）。
  - **按项目任务完成情况**：柱状图，显示各项目的任务总数与完成数（可选）。
  - **近期团队动态表格**：列（时间、成员、类型、内容），每行显示操作记录，支持分页。
  - 底部显示“数据更新于”时间戳，带手动刷新按钮。

![团队空间-统计](E:\Codex\Notask-Flow\img\团队空间-统计.png)

#### 3.2.8 空间设置页面

- **左侧二级菜单**：基本信息、成员管理、高级设置（可选）。
- **右侧内容**：
  - **基本信息**：空间名称（可编辑）、空间类型（团队空间，不可修改）、创建人、创建时间、解散空间按钮（仅所有者，二次确认）。
  - **成员管理**：成员列表表格（头像、姓名、邮箱、角色、加入时间）。操作：邀请成员（弹窗输入邮箱选择角色）、修改角色（下拉）、移除成员（红色按钮）。邀请支持链接或邮件。增加“待审核申请”标签页（见第 8 节权限体系）。
  - **高级设置**：新成员加入需要审核（开关）、允许成员邀请新成员（开关）、是否允许成员创建项目（开关）。

![团队空间-空间设置](E:\Codex\Notask-Flow\img\团队空间-空间设置.png)

### 3.3 通用组件与细节

#### 3.3.1 通知中心（两个空间功能统一，但是主题颜色不同）

- 左侧分类标签：全部、未读、任务相关、笔记相关、系统通知，显示未读数。
- 右侧通知列表：每条通知包含图标（任务/笔记/系统）、内容摘要、相对时间（“10分钟前”或“05-28”）。未读通知背景浅蓝色。每条右侧有“标记已读”按钮。顶部有“全部标记已读”、“清空已读”按钮。点击通知内容跳转到相应资源。分页。

![个人空间-通知中心](E:\Codex\Notask-Flow\img\个人空间-通知中心.png)

![团队空间-通知中心](E:\Codex\Notask-Flow\img\团队空间-通知中心.png)

#### 3.3.2 全局搜索

- 快捷键 `Ctrl+K` 唤起模态框。
- 搜索结果分为笔记、任务、待办、项目四个标签页，显示匹配项标题、所属空间、高亮关键词。点击结果跳转。
- 支持按空间筛选（下拉选择）。

![个人空间-全局搜索](E:\Codex\Notask-Flow\img\个人空间-全局搜索.png)

#### 3.3.3 附件管理

- 上传：笔记编辑器内拖拽/粘贴图片；任务详情附件区点击上传。显示上传进度。
- 预览：图片附件可点击放大预览；文档附件显示下载按钮。
- 删除：附件列表每项右侧删除图标，二次确认。

#### 3.3.4 空状态

- 个人空间：手绘插画（例如一杯咖啡、一本书、猫咪），文案“还没有笔记，写点什么吧”，按钮“新建笔记”。
- 团队空间：几何风格插画（例如协作图标），文案“暂无任务，点击创建第一个团队任务”。项目列表空状态：“还没有项目，点击创建项目”。

---

## 4. 切换体验总结

用户在顶部切换器点击不同空间时，界面发生以下变化：

| 元素     | 个人空间 → 团队空间                             | 团队空间 → 个人空间   |
| -------- | ----------------------------------------------- | --------------------- |
| 背景色   | 暖白 → 浅灰                                     | 浅灰 → 暖白           |
| 卡片圆角 | 16px → 8px                                      | 8px → 16px            |
| 卡片阴影 | 有阴影 → 无阴影，变为边框                       | 边框 → 阴影           |
| 内容宽度 | 居中 800px → 全宽                               | 全宽 → 居中 800px     |
| 字体     | 行高 1.8，标题衬线 → 行高 1.4，标题非衬线       | 反向                  |
| 侧边栏   | 显示随机漫步、每日一句 → 显示成员头像、团队动态 | 反向                  |
| 主视图   | 笔记编辑器 → 任务看板                           | 任务看板 → 笔记编辑器 |
| 顶部栏   | 无成员头像组 → 显示成员头像组                   | 反向                  |

切换动画使用 GSAP 平滑过渡，整体感知为“两个不同的应用”而不是简单换肤。

---

## 5. 主题风格 UI 图（个人空间）

![个人空间-暗夜主题](E:\Codex\Notask-Flow\img\个人空间-暗夜主题.png)

![个人空间-海洋主题](E:\Codex\Notask-Flow\img\个人空间-海洋主题.png)

![个人空间-森林主题](E:\Codex\Notask-Flow\img\个人空间-森林主题.png)

---

## 6. 文档分享链接功能

**适用模块**：个人空间的笔记编辑器、团队空间的协作文档（笔记）详情页。

### 6.1 分享入口与 UI

- **位置**：笔记编辑器顶部工具栏，位于“历史版本”图标右侧，图标为 🔗 或“分享”文字按钮。
- **交互**：点击打开分享设置抽屉（右侧滑出，宽度 400px）或模态框。
- **基本功能**：
  - 生成分享链接：点击“创建分享链接”后生成唯一短链接，显示链接文本与“复制链接”按钮。
  - 访问权限设置（单选）：
    - 公开：任何人持有链接可访问（只读，不可编辑）。
    - 团队内：仅同一团队空间成员可访问（仅对团队空间笔记有效；个人空间笔记不支持此选项）。
    - 指定用户：通过邮箱或用户名添加允许访问的人员列表（最多 20 人）。
  - 有效期设置：可选择“7天后过期”“30天后过期”“永久有效”或自定义日期时间。
  - 密码保护（可选）：开关，设置访问密码（4-6位数字/字母），访问时需输入。
- **已生成的链接管理**：
  - 在抽屉内展示该笔记已生成的分享链接列表（最多 5 个），每个链接显示权限、有效期、访问次数（PV），右侧有“停用”按钮和“复制链接”。
  - 停用后链接立即失效，可重新生成新链接。

### 6.2 访问端体验

- **页面**：独立的无登录态视图，标题为“[笔记标题] · Notask Flow 分享”。
- **展示**：只读渲染的笔记内容（支持 Markdown 富文本展示），顶部显示“由 [空间名称] 的 [分享者昵称] 分享 • 仅查看”。若受密码保护，显示密码输入框，验证通过后展示内容。
- **CTA（可选）**：底部提示“加入 Notask Flow 协作”按钮，点击跳转至注册/登录页。
- **与后端交互**：后端需记录分享 ID、笔记 ID、权限、有效期、密码哈希、创建人及访问日志。

---

## 7. 团队加入机制与个人团队管理

### 7.1 注册/登录时的团队加入

- **场景**：新用户注册时，输入邮箱/用户名/密码后，可扩展一个可选步骤“加入现有团队”。
- **UI**：卡片形式，提供两种方式：
  - **使用邀请码**：输入团队邀请码（6-8位字母数字），点击验证，后端返回团队名称和创建人，用户确认后即成为该团队的“成员”角色。
  - **申请加入团队**：输入团队邮箱域名（如 `@example.com`）或团队全名（模糊搜索），系统列出匹配的团队，用户选择后需填写申请理由（选填）。申请发送给团队管理员（在团队“成员管理”页面内通知栏可见）。
- **跳过**：未加入任何团队的用户直接进入个人空间，后续仍可在顶部空间切换器或空间设置中加入团队。

### 7.2 个人创建团队

- **入口**：在空间切换器的下拉面板内，“+ 新建团队空间”链接。
- **创建弹窗**：
  - 团队名称（必填，2–30 字符）
  - 团队唯一标识（自动生成如 `team-xxxx`，可手动修改，用于邀请链接）
  - 团队头像（可选，默认首字母图标）
  - 初始角色：创建者自动成为“所有者”，默认拥有全部权限。
- **创建后**：立即切换至新团队空间，展示空任务看板和协作文档。创建者可在空间设置中邀请成员。

### 7.3 加入他人团队（非注册时）

- **入口**：全局任意时刻，通过以下方式触发：
  - 点击他人分享的团队邀请链接（URL 格式 `https://[domain]/invite/[teamCode]`）
  - 在空间切换器下拉面板底部点击“加入团队”按钮（模态框）
- **流程**：
  1. 输入邀请码（8 位字符），系统展示团队名称、所有者。
  2. 点击“确认加入”，若团队无需审核（由管理员在空间设置中配置“新成员加入需要审核”开关），则直接成为“成员”；若需要审核，则发送申请，管理员在成员管理页面批准后生效。
- **审批通知**：管理员会在通知中心收到“XX 申请加入团队”的待办提醒，支持通过/拒绝。

### 7.4 退出团队

- **入口**：空间设置 → 基本信息 → “退出团队”按钮（普通成员可见；所有者不可见，仅可见“解散团队”）。
- **确认弹窗**：警告“退出后您将无法访问该空间的任何资源，且可能被移出协作中的任务/笔记”。确认后从成员列表中移除，个人空间不再显示该团队。

---

## 8. 权限体系细化

### 8.1 角色定义

| 角色     | 说明                                                     | 可赋予人数 |
| -------- | -------------------------------------------------------- | ---------- |
| 所有者   | 团队创建者，默认拥有所有权限，可解散团队、转让所有权     | 1 人       |
| 管理员   | 除解散团队、转让所有权外，拥有管理成员、设置空间的能力   | 不限       |
| 成员     | 正常使用任务、文档、待办等功能，可创建内容但不能管理成员 | 不限       |
| 只读访客 | 仅查看任务、文档，不可编辑、创建、删除                   | 不限       |

### 8.2 权限矩阵

| 操作                      | 所有者 | 管理员 | 成员                | 只读访客 |
| ------------------------- | ------ | ------ | ------------------- | -------- |
| **空间管理**              |        |        |                     |          |
| 编辑空间基本信息          | ✅      | ✅      | ❌                   | ❌        |
| 邀请成员                  | ✅      | ✅      | ❌                   | ❌        |
| 移除成员                  | ✅      | ✅      | ❌                   | ❌        |
| 修改成员角色（含升降级）  | ✅      | ✅      | ❌                   | ❌        |
| 审批加入申请              | ✅      | ✅      | ❌                   | ❌        |
| 转让所有权                | ✅      | ❌      | ❌                   | ❌        |
| 解散团队                  | ✅      | ❌      | ❌                   | ❌        |
| **项目管理**              |        |        |                     |          |
| 创建/编辑/删除项目        | ✅      | ✅      | ✅                   | ❌        |
| 管理项目成员（添加/移除） | ✅      | ✅      | ❌（仅项目负责人可） | ❌        |
| 设置项目负责人            | ✅      | ✅      | ❌                   | ❌        |
| **任务看板**              |        |        |                     |          |
| 创建/编辑/删除任务        | ✅      | ✅      | ✅                   | ❌        |
| 变更任务状态（拖拽卡片）  | ✅      | ✅      | ✅                   | ❌        |
| 分配责任人/协作者         | ✅      | ✅      | ✅                   | ❌        |
| 查看所有任务              | ✅      | ✅      | ✅                   | ✅        |
| **协作文档（团队笔记）**  |        |        |                     |          |
| 创建/编辑/删除文档        | ✅      | ✅      | ✅                   | ❌        |
| 查看文档                  | ✅      | ✅      | ✅                   | ✅        |
| 评论文档                  | ✅      | ✅      | ✅                   | ❌        |
| 历史版本恢复              | ✅      | ✅      | ✅                   | ❌        |
| 分享文档（生成链接）      | ✅      | ✅      | ✅                   | ❌        |
| **待办（团队）**          |        |        |                     |          |
| 创建/编辑/删除待办        | ✅      | ✅      | ✅                   | ❌        |
| 完成他人创建的待办        | ✅      | ✅      | ✅                   | ❌        |
| 查看全部待办              | ✅      | ✅      | ✅                   | ✅        |
| **成员与通知**            |        |        |                     |          |
| 查看成员列表              | ✅      | ✅      | ✅                   | ✅        |
| @提及任意成员             | ✅      | ✅      | ✅                   | ❌        |
| 接收团队通知              | ✅      | ✅      | ✅                   | ✅        |
| **统计报表**              |        |        |                     |          |
| 查看团队统计仪表盘        | ✅      | ✅      | ✅                   | ✅        |
| 导出报表（CSV/PDF）       | ✅      | ✅      | ❌                   | ❌        |

### 8.3 与个人空间的差异说明

- 个人空间不存在角色矩阵，用户为唯一所有者，所有操作均允许。
- 当用户创建团队后，其个人空间仍独立存在，团队空间不影响个人笔记/待办。

### 8.4 UI 补充说明

- **空间设置 → 成员管理**：表格中增加“角色”下拉选择（仅管理员/所有者可见编辑），对于所有者行禁用修改。移除成员时需二次确认，并提示“该成员创建的任务/文档将被保留，创建者显示为‘已移除成员’”。
- **待审核申请标签页**：成员管理页面新增 Tab“成员列表” / “待审核申请”。待审核列表每行显示申请者头像、邮箱、申请时间、申请理由，右侧“通过”和“拒绝”按钮。
- **任务/文档详情权限提示**：当只读访客试图编辑时，显示 toast 提示“您只有只读权限，如需编辑请联系管理员”。
- **邀请成员模态框**：支持输入邮箱列表（批量，每行一个），选择角色（默认“成员”），并可勾选“需要审核”。生成邀请链接并复制，或通过邮件发送邀请（需后端邮件服务）。

---

## 9. 登录与注册页面

### 9.1 整体风格

- 独立于应用主体，页面采用渐变背景或极简浅色背景，品牌 Logo 突出。
- 响应式设计，卡片式表单居中，圆角 24px，阴影柔和。
- 支持亮色/暗色模式（跟随系统偏好）。

### 9.2 登录页面

**URL**：`/login`

**布局**：

- 左侧（或上方）：品牌 Logo 及 “Notask Flow” 标语（如“任务与笔记的流畅协作”）。
- 右侧（或下方）：登录表单。

**表单字段**：

- 用户名/邮箱（文本输入框，支持自动补全）
- 密码（密码输入框，显示/隐藏切换）
- 记住我（复选框）
- 登录按钮（主色，加载状态）
- 忘记密码？链接（点击跳转至重置密码页）

**扩展链接**：

- 没有账号？立即注册（跳转注册页）
- 加入团队（跳转注册页并附带团队邀请码？可留作后续）

**错误处理**：

- 用户名/密码错误：显示红色提示条。
- 账号被禁用：提示联系管理员。

**附加功能**：

- 在登录页的 URL 参数中可携带 `redirect` 参数，登录成功后跳转到原请求页面。

![登录界面](E:\Codex\Notask-Flow\img\登录界面.png)

### 9.3 注册页面

**URL**：`/register`

**布局**：与登录页一致，但表单内容不同。

**表单字段**：

- 昵称（真实姓名或显示名，2-20 字符）
- 用户名（唯一标识，用于登录和 @提及，字母数字下划线，4-20 字符）
- 邮箱（必填，需验证格式）
- 密码（至少 8 位，包含字母和数字）
- 确认密码
- 团队邀请码（可选输入框，提示“如果有团队邀请码，请输入”）

**注册流程**：

1. 填写基本信息，勾选同意《用户协议》和《隐私政策》（必选）。
2. 点击“注册”按钮，发送验证邮件到填写的邮箱。
3. 展示“验证邮件已发送”页面，用户点击邮件中的链接完成验证。
4. 验证成功后，自动跳转到登录页或直接登录进入应用（根据产品策略）。

**团队加入处理**：

- 如果用户在注册时输入了有效的邀请码，则在邮箱验证通过并首次登录后，自动将该用户添加为对应团队的“成员”角色（若无审核要求）或进入待审核状态。
- 若邀请码无效，给予提示但仍可继续完成注册（仅创建个人空间）。

**错误处理**：

- 用户名/邮箱已存在：实时校验提示。
- 密码强度不足：显示强度条和具体要求。
- 邀请码无效：提示“邀请码无效，将继续创建个人账户”。

![注册界面](E:\Codex\Notask-Flow\img\注册界面.png)

### 9.4 忘记密码 / 重置密码

**URL**：`/forgot-password`

**步骤**：

1. 输入注册邮箱，点击“发送重置链接”。
2. 系统发送含唯一 token 的链接到邮箱。
3. 用户点击链接进入重置密码页（`/reset-password?token=xxx`），输入新密码并确认。
4. 提交后重置成功，跳转登录页。

**交互注意事项**：

- 重置 token 有效期 1 小时，过期需重新发送。
- 重置成功后，所有已登录设备应强制登出（后端实现）。

### 9.5 账号验证与安全

- **邮箱验证**：注册后必须验证邮箱才能使用全部功能（未验证用户可登录但只能浏览，无法创建笔记/任务等）。
- **登出**：前端清除本地 token，跳转登录页。

---

## 10. 路由设计

| 路径                           | 组件                | 权限/说明                      |
| ------------------------------ | ------------------- | ------------------------------ |
| `/login`                       | LoginView           | 未登录可访问                   |
| `/register`                    | RegisterView        | 未登录可访问                   |
| `/forgot-password`             | ForgotPasswordView  | 未登录可访问                   |
| `/reset-password`              | ResetPasswordView   | 未登录可访问                   |
| `/app`                         | Layout (默认重定向) | 需登录，根据用户默认空间跳转   |
| `/app/notes`                   | NoteView            | 笔记主页                       |
| `/app/notes/:noteId`           | NoteDetail          | 编辑/查看单篇笔记              |
| `/app/tasks`                   | TaskView            | 任务列表/看板                  |
| `/app/tasks/:taskId`           | TaskDetail          | 任务详情                       |
| `/app/todos`                   | TodoView            | 待办列表                       |
| `/app/stats`                   | PersonalStatsView   | 个人统计                       |
| `/app/spaces/:spaceId/stats`   | TeamStatsView       | 团队空间统计（仅团队空间有效） |
| `/app/notifications`           | NotificationView    | 通知中心                       |
| `/app/settings`                | UserSettingsView    | 个人设置（含子路由）           |
| `/app/space/:spaceId/settings` | SpaceSettingsView   | 空间设置（仅团队空间）         |
| `/app/projects`                | ProjectListView     | 项目列表                       |
| `/app/projects/:projectId`     | ProjectDetailView   | 项目详情（含子 Tab）           |
| `/public/notes/:shareCode`     | PublicNoteView      | 公开笔记（只读，无需登录）     |
| `/invite/:teamCode`            | TeamInviteView      | 团队邀请页面（可加入团队）     |
| `*`                            | NotFoundView        | 404                            |

**路由守卫**：未登录访问需认证路由 → 跳转 `/login`；登录后自动获取用户空间列表，若当前 `spaceId` 无效则重定向到第一个可用空间；团队空间特有页面需校验当前空间类型。

---

## 11. 状态管理（Pinia Store）

| Store 名称          | 职责                                                         |
| ------------------- | ------------------------------------------------------------ |
| `userStore`         | 用户信息（ID、用户名、邮箱、头像）、登录状态、Token 管理     |
| `spaceStore`        | 用户可访问的空间列表、当前选中的空间对象、切换空间方法、当前空间权限码列表 |
| `noteStore`         | 当前空间的笔记本树、笔记列表、当前编辑笔记内容（缓存）       |
| `taskStore`         | 任务列表筛选参数、当前任务详情缓存、任务成员操作、看板列配置 |
| `todoStore`         | 待办列表缓存、未完成数量、待办筛选条件（标签、来源等）       |
| `projectStore`      | 项目列表、当前项目详情、项目成员、项目筛选条件               |
| `notificationStore` | 未读数量、通知列表分页、分类选中状态                         |
| `uiStore`           | 侧边栏折叠状态、主题（亮色/暗色/跟随系统）、全局加载状态、通知偏好设置 |

**持久化**：`userStore` 中的 Token 和用户基本信息使用 `localStorage` 或 `pinia-plugin-persistedstate` 持久化。

---

## 12. API 调用规范

### 12.1 基础配置

- 基准 URL：`/api/v1`（可通过环境变量配置）
- 请求拦截器：自动在 `Authorization` 头添加 `Bearer ${token}`
- 响应拦截器：统一处理 `code` 字段，非 `200` 时弹出错误提示；对 `401` 清除本地 Token 并跳转登录页；对 `403` 提示权限不足。

### 12.2 空间上下文注入

所有需要 `spaceId` 的 API 请求，在封装时自动从 `spaceStore.currentSpace.id` 获取并替换路径中的 `{spaceId}`。

### 12.3 分页参数统一

所有列表请求携带 `pageNum`、`pageSize`（默认值：1, 10），响应按后端统一格式处理。

### 12.4 错误码映射

前端维护错误码与用户提示的映射表（如 `2003` → “该职责已被认领”）。

---

## 13. UI/UX 要求

### 13.1 布局与响应式

- 侧边栏宽度 260px，可收起至 64px（仅显示图标）
- 主内容区最小宽度 800px，低于此宽度时侧边栏自动隐藏（移动端适配）
- 笔记本树支持拖拽调整层级（需调用后端更新父节点 API）

### 13.2 交互反馈

- 所有异步操作需显示 loading 状态（按钮、骨架屏或全局遮罩）
- 表单提交成功/失败使用顶部通知或弹窗提示
- 删除、解散等危险操作需二次确认

### 13.3 主题

支持亮色/暗色主题切换（Element Plus 主题变量 + 自定义 CSS），跟随系统选项。个人空间额外支持暖阳/森林/海洋/暗夜预设主题。

### 13.4 性能

- 路由懒加载
- 笔记列表虚拟滚动（笔记数量多时）
- 笔记编辑内容防抖保存（间隔 3 秒自动保存草稿）

### 13.5 动画与过渡规范

- **页面切换**：淡入淡出 + 水平微位移（8px），时长 200ms。
- **列表项动画**：使用 `<TransitionGroup>`，进入/离开动画（透明度+垂直位移），时长 250ms。
- **微交互**：按钮悬停（0.2s 缩放/背景色变化），卡片悬停（阴影加深、上浮），模态框/抽屉（透明度+缩放，200ms）。
- **拖拽反馈**：半透明副本 + 蓝色虚线边框（目标位置）。
- **空间切换动画**：见第 4 节，使用 GSAP 插值 CSS 变量。
- **性能与可访问性**：遵循 `prefers-reduced-motion`，移动端避免使用 `transform` 之外的属性做动画。

### 13.6 通用组件样式

| 元素      | 样式                                      |
| --------- | ----------------------------------------- |
| 主按钮    | 品牌蓝紫色（#6366f1），圆角 8px，悬停加深 |
| 次要按钮  | 灰色边框，白色背景                        |
| 输入框    | 圆角 8px，边框 #e2e8f0，聚焦品牌色边框    |
| 卡片      | 圆角 12px，白色背景，浅阴影，悬停加深     |
| 抽屉/弹窗 | 圆角 16px，平滑动画（0.2s）               |
| 拖拽占位  | 半透明副本 + 蓝色虚线边框                 |
| 空状态    | 插画 + 引导文字 + 行动按钮                |

### 13.7 响应式断点

- **≥1440px**：标准多栏布局
- **1024px–1439px**：侧边栏可折叠，编辑器支持全宽切换
- **768px–1023px**：隐藏笔记本树（汉堡菜单呼出），笔记列表和编辑器两栏
- **<768px**：单栏 + 底部 Tab 栏，侧边栏/抽屉式呼出

---

## 14. 非功能需求

### 14.1 浏览器兼容性

- 支持 Chrome、Edge、Firefox 最新两个版本，暂不考虑 IE 11。

### 14.2 代码规范

- 使用 ESLint + Prettier，Git 提交前执行 lint-staged
- 组件采用 `<script setup>` 语法
- 命名：组件使用 PascalCase，组合式函数使用 `use` 前缀

### 14.3 工程化

- 开发环境支持 HMR
- 生产构建启用 gzip 压缩
- 环境变量：`.env.development`、`.env.production`

### 14.4 错误监控

- 集成 Sentry（可选）或前端日志上报
- 捕获未处理的 Promise 异常并提示

---

## 15. 与后端的联调要点

1. **权限模型**：前端必须正确传递 `spaceId`，并在路由/组件内根据当前空间角色控制 UI 元素显示。
2. **任务状态机**：任务整体状态和成员状态变更需严格遵循后端枚举允许的流转，前端禁用不允许的按钮。
3. **乐观锁**：任务成员认领接口可能返回 `409` 冲突，前端需提示“已被认领”并刷新列表。
4. **文件上传**：使用 FormData 上传，先调用 `/attachments/upload` 获得附件 ID，再调用绑定接口关联到笔记/任务。
5. **WebSocket 实时通知（可选）**：若需实时推送未读数量，可集成 STOMP over WebSocket，订阅 `/user/queue/notifications`。
6. **项目相关 API**：需要后端提供项目的 CRUD、关联任务/文档、项目成员管理等接口。

---

## 16. 附录：关键界面参考（整合）

以下为参考图片文件夹路径，实际设计时需根据最新 UI 输出：

`E:\Codex\Notask-Flow\img`

---

**本需求说明书完整覆盖前端界面、交互、权限及用户流程，开发与设计应以此为准。**